# Генератор паролей на Python

Программа генерирует случайные пароли с опцией специальных символов, проверяет на точки/запятые и сохраняет их в файл на рабочем столе.

## Установка
- Установите Python 3.6+.
- Запустите: `Code.py`.

## Функции
- Запрос длины пароля (мин. 8).
- Опция специальных символов.
- Сохранение в `passwords.txt` на Desktop.
- Проверка на '.' и ','.
